name: My First Action

on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Clone repository
        uses: https://github.com/tumsrichomson/${{ github.repository }}.git

      - name: Show message
        run: echo "Hello, GitHub Actions!"

      - name: Show files
        run: ls -la
